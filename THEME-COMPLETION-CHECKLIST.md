# WordPressテーマ化 完了チェックリスト

このチェックリストは、本プロジェクトの静的HTMLをWordPressテーマへ移植した後、制作担当者が納品可能な品質に達しているかを確認するためのものです。

このプロジェクトでは、PHPテンプレート階層を使用するクラシックテーマを制作します。
投稿・固定ページの編集にはブロックエディターを利用できる前提とし、エディター用CSSを必須、`theme.json` をブロック設定とエディター／フロントのデザイン整合を補助する任意ファイルとして扱います。フルサイト編集（FSE）を前提とするブロックテーマにはしません。

## チェック方法

- `[ ]`: 未確認
- `[x]`: 確認済み
- `N/A`: 対象外。対象外にした理由を備考へ記載する
- 目視だけでなく、可能な項目は実際のWordPress環境、管理画面、ブラウザ、検証ツールで確認する
- 重大な未完了項目が1つでもある状態を「テーマ化完了」としない

## 検収情報

- テーマ名: バイバイコムズ ホビー買取
- テーマスラッグ: `buybuycoms-hobby`
- テキストドメイン: `buybuycoms-hobby`
- テーマバージョン: `0.1.0`
- 検収対象コミット:
- WordPressバージョン:
- PHPバージョン:
- 検証URL:
- 検証日: 2026-07-31（進捗資料更新時点）
- 検証担当:
- 対象ブラウザ／端末:
- 必須プラグインとバージョン:
- 備考: リポジトリのコード確認と作業ログに基づく途中確認。実環境、境界値、複数ブラウザ、公開前検証は未完了。

---

## 1. 完了条件と仕様

- [x] テーマ化の対象ページ、投稿タイプ、タクソノミー、機能が一覧化されている
- [x] 静的HTMLのどの部分をWordPressで動的化したか記録されている
- [ ] 管理画面から編集する項目と、テーマ内に固定する項目が整理されている
- [ ] 必須プラグイン、推奨プラグイン、プラグインなしで動作する範囲が明記されている
- [ ] カスタム投稿タイプ、タクソノミー、カスタムフィールドの登録場所と責務が明確である
- [ ] テーマ切替後も保持すべき業務機能を、テーマへ不適切に実装していない
- [ ] 対応するWordPress、PHP、ブラウザのバージョン範囲が決まっている
- [x] ユーザーから受けた仕様と、実装上の判断・保留事項が記録されている

## 2. テーマ基本ファイル

- [x] PHPテンプレートを主体とするクラシックテーマとして構成されている
- [x] ブロックテーマ用の `templates/*.html` や `parts/*.html` を意図せず混在させていない
- [x] `style.css` がテーマ直下に存在する
- [x] `style.css` のTheme Name、Version、Text Domain等のヘッダーが正しい
- [x] テキストドメインがテーマスラッグと一致している
- [ ] `index.php` が存在し、フォールバックとして致命的な表示崩れなく機能する
- [x] `functions.php` が存在し、責務が大きい処理は `inc/` 等へ分割されている
- [x] `header.php`、`footer.php` が存在し、共通表示が各テンプレートに重複していない
- [ ] `screenshot.png` が実際のテーマデザインを表している
- [ ] `theme.json` の採用有無と、クラシックテーマ内で利用する目的が明確である
- [ ] `theme.json` を採用する場合、JSON構文と使用スキーマが検証されている
- [ ] `404.php`、`search.php`、`searchform.php` など必要なフォールバックが用意されている
- [ ] コメントを使用する場合は `comments.php` とコメントページングが機能する
- [ ] ライセンス、第三者素材、フォント、ライブラリの利用条件が確認されている
- [ ] テーマ内にWordPress本体、不要なプラグイン、秘密情報を含めていない

## 3. WordPress必須フック・テーマサポート

- [x] 各テンプレートが `get_header()`、`get_footer()` 等で共通テンプレートを呼び出している
- [x] `wp_head()` が `</head>` の直前で呼ばれている
- [x] `wp_body_open()` が `<body>` の直後で呼ばれている
- [x] `wp_footer()` が `</body>` の直前で呼ばれている
- [x] `language_attributes()` が使用されている
- [x] `bloginfo( 'charset' )` が使用されている
- [x] `body_class()` が使用されている
- [ ] `post_class()` が投稿単位の適切な要素で使用されている
- [x] `title-tag` のテーマサポートが登録され、固定 `<title>` と二重出力になっていない
- [x] `post-thumbnails` が必要な投稿タイプで利用できる
- [x] 必要なHTML5サポートが登録されている
- [x] `responsive-embeds`、`align-wide`、`wp-block-styles` の採用要否を確認している
- [x] カスタムロゴを使用する場合、`custom-logo` が適切に設定されている
- [x] メニュー位置が `register_nav_menus()` で登録されている
- [ ] ウィジェットエリアを使用する場合、`register_sidebar()` と `dynamic_sidebar()` が適切に実装されている
- [ ] テーマ設定が必要な場合、WordPress標準のカスタマイザーや設定API等を優先している
- [x] テーマセットアップ処理が `after_setup_theme` など適切なフックで実行されている
- [ ] 翻訳ファイルを同梱する場合、テキストドメインが適切に読み込まれている
- [ ] 不要なWordPress標準機能を理由なく解除していない

## 4. ブロックエディターと編集画面

### エディターCSS

- [x] `add_theme_support( 'editor-styles' )` と `add_editor_style()` が適切に設定されている
- [x] エディター用CSSが `after_setup_theme` のタイミングで登録されている
- [x] `asset/css/tokens.css` の必要なカスタムプロパティがエディター内でも利用できる
- [x] フロント用リセットCSSを無条件にエディターへ流し込み、管理画面UIを壊していない
- [ ] エディター用CSSが `.editor-styles-wrapper` 等の適切なスコープを考慮している
- [ ] フロントとエディターで、本文のフォント、文字色、背景色、行間が概ね一致する
- [ ] 見出し `h1`〜`h6` のサイズ、太さ、余白がエディターでも確認できる
- [ ] 段落、リンク、リスト、引用、表、ボタン、区切り線の見た目がエディターでも確認できる
- [ ] 画像、ギャラリー、キャプション、動画、埋め込みの幅と余白がエディターでも確認できる
- [ ] 共通コンポーネント相当のブロックスタイルが、エディターとフロントの両方へ反映される
- [ ] CSSの読込失敗や404が管理画面のNetworkパネルにない
- [ ] エディターに不要なフロント用JavaScriptを読み込んでいない

### `theme.json` とブロック設定

- [ ] `theme.json` の `settings.layout.contentSize` と `wideSize` が実際のデザイン幅と整合している
- [ ] 最大幅 `1200px` の方針がフロントとエディターで矛盾していない
- [ ] 使用可能な色、フォントサイズ、余白等が `tokens.css` の設計と矛盾していない
- [ ] エディターが生成するWordPressプリセットと既存CSS変数の役割が整理されている
- [ ] テーマで使用しない任意色、任意フォントサイズ等のUIを許可するか判断している
- [ ] 標準ブロックのスタイルは、可能な範囲で `theme.json` またはブロック用CSSとして管理されている
- [ ] `alignwide`、`alignfull`、左右配置を許可する場合、フロントとエディターの両方で崩れない
- [ ] ブロック間余白がフロントとエディターで大きく乖離していない
- [ ] 背景色を設定したブロックで文字が読める
- [ ] 管理者が選択できる設定範囲が、サイトのデザインを容易に壊す状態になっていない

### 投稿編集の実用性

- [ ] 実際の投稿内容を編集画面に読み込み、公開画面と比較している
- [ ] エディターの本文幅が公開画面の本文幅を適切に再現している
- [ ] 長い見出し、長いURL、長い英単語、日本語と英数字混在で横にはみ出さない
- [ ] 空ブロック、連続した見出し、深い階層のリストでも編集不能にならない
- [ ] カスタムブロックを使う場合、プレビュー、入力項目、未入力状態が確認できる
- [ ] 再利用する構成は、必要に応じてブロックパターンとして選択できる
- [ ] ブロックパターン内に本番と誤認するダミーURLや連絡先が残っていない
- [ ] エディターだけで必要な説明や補助表示が、公開画面へ出力されていない
- [ ] Classic Editorプラグインも対応対象の場合、TinyMCE上でも本文スタイルを確認している

## 5. 静的HTMLからの移植確認

### 静的制作からテーマへの切替

- [x] `pages/` を静的な完成見本、WordPressのPHPを本番実装として区別している
- [x] `[data-hb-include]` の全参照箇所についてPHP側の移植先を確認している
- [x] WordPressの公開画面に `[data-hb-include]` 属性や空の読込用要素が残っていない
- [x] `asset/js/include-components.js` をWordPressの公開画面でenqueueしていない
- [x] ヘッダーとフッターをJavaScriptの `fetch()` に依存せずPHPで出力している
- [x] その他の共有部品を `get_template_part()` 等でサーバー側出力している
- [x] 静的HTMLの `<head><style>` 内のCSSを外部CSSへ移している
- [x] PHPテンプレート、`header.php`、テンプレートパーツ内に大量のインラインCSSが残っていない
- [x] 静的HTML末尾のインラインJavaScriptを責務別の外部ファイルへ移している
- [ ] 静的制作用の相対リンク、ダミーURL、固定タイトル、開発用属性を整理している
- [ ] 静的HTMLとWordPress表示を比較し、移植による要素欠落がない

### ページ

- [x] `pages/front.html` 相当のフロントページが実装されている
- [x] `pages/page-reason.html` 相当の固定ページが実装されている
- [x] `pages/page-privacy.html` 相当の固定ページが実装されている
- [x] `pages/page-genre-list.html` 相当の固定ページが実装されている
- [x] `pages/page-flow.html` 相当の固定ページが実装されている
- [x] `pages/page-faq.html` 相当の固定ページが実装されている
- [x] `pages/page-contact.html` 相当の固定ページが実装されている
- [x] `pages/page-company.html` 相当の固定ページが実装されている
- [x] 固定ページのスラッグとテンプレートの対応が管理・記録されている

### 投稿一覧・詳細・分類

- [x] `pages/archive-purchase-record.html` 相当の一覧が実装されている
- [x] `pages/single-purchase-record.html` 相当の詳細が実装されている
- [x] `pages/archive-info.html` 相当の一覧が実装されている
- [x] `pages/single-info.html` 相当の詳細が実装されている
- [x] `pages/archive-column.html` 相当の一覧が実装されている
- [x] `pages/single-column.html` 相当の詳細が実装されている
- [x] `pages/taxonomy-genre.html` 相当のタクソノミー一覧が実装されている
- [ ] 各投稿タイプとタクソノミーのスラッグが仕様どおりである
- [ ] 一覧から詳細、詳細から分類・一覧への導線が正しい
- [ ] 投稿0件、1件、複数件、ページネーション発生時を確認している

### 共通コンポーネント

- [x] `components/header.html` 相当が共通化されている
- [x] `components/footer.html` 相当が共通化されている
- [x] `components/footer-cta.html` 相当が共通化されている
- [x] `components/parts-cta.html` 相当が共通化されている
- [x] `components/blog-card.html` 相当が共通化されている
- [x] `components/purchase-methods.html` 相当が共通化されている
- [x] `components/purchase-cases.html` 相当が共通化されている
- [x] `components/purchase-price-table.html` 相当が共通化されている
- [x] `components/genre-table.html` 相当が共通化されている
- [x] `components/flow-tab.html` 相当が共通化されている
- [x] `components/customer-reviews.html` 相当が共通化されている
- [x] 同じHTMLを複数テンプレートへコピーせず、`get_template_part()` 等で再利用している
- [ ] テンプレートパーツへ渡す引数と初期値が明確である
- [ ] 同じテンプレートパーツを1ページ内で複数回呼び出してもIDや状態が競合しない

## 6. 動的データと管理画面運用

- [ ] サイト名、ロゴ、メニューなどが適切なWordPress設定と連動している
- [ ] 固定ページのタイトルと本文を変更すると、意図した箇所へ反映される
- [ ] 投稿タイトル、本文、抜粋、公開日、更新日、投稿者の使用要否が仕様どおりである
- [ ] アイキャッチの推奨サイズが決まり、未設定時も崩れない
- [ ] 添付画像は可能な範囲でWordPressの画像IDから出力し、`srcset` と `sizes` が利用される
- [ ] 画像の代替テキストがメディア設定または適切な固定値から出力される
- [ ] カテゴリー、タグ、タクソノミーが空の場合も不自然な記号や空要素が出ない
- [ ] カスタムフィールドが未入力、一部入力、全入力の各状態で崩れない
- [ ] 繰り返し項目が0件、1件、多数の場合を確認している
- [ ] 管理画面の項目名、説明、入力例が運用担当者に理解できる
- [ ] 必須入力と任意入力が区別されている
- [ ] 入力可能な文字数、画像比率、推奨サイズなどの運用ルールが文書化されている
- [ ] 管理者、編集者など実際の運用権限で必要な編集ができる
- [ ] 非公開、下書き、予約投稿、パスワード保護の扱いを確認している
- [ ] プレビュー表示が本番表示と大きく乖離しない

## 7. テンプレート階層・ループ

- [x] クラシックテーマのPHPテンプレート階層に従っている
- [x] `get_header()`、`get_footer()`、`get_sidebar()`、`get_template_part()` を目的に応じて使用している
- [ ] `front-page.php` とホーム／投稿ページ設定の関係が仕様どおりである
- [ ] `home.php`、`page.php`、`single.php`、`archive.php`、`taxonomy.php` のフォールバックを確認している
- [ ] カスタム投稿タイプ用・タクソノミー用テンプレート名が正しい
- [x] メインクエリを不要に置き換えていない
- [x] `query_posts()` を使用していない
- [ ] 独自クエリは目的、件数、並び順、ページングが仕様どおりである
- [ ] 独自 `WP_Query` 後に必要な `wp_reset_postdata()` を実行している
- [ ] `pre_get_posts` を使う場合、管理画面や別クエリへ誤適用していない
- [ ] N+1クエリや同一データの不要な反復取得がない
- [ ] ページネーションが先頭、中間、最終、存在しない場合で正常である
- [ ] 複数ページに分割した投稿を扱う場合、`wp_link_pages()` が機能する
- [ ] 存在しないページ番号や投稿URLで適切に404となる

## 8. URL・リンク・アセット

- [x] テーマURL、ホームURL、投稿URLを固定文字列で記述していない
- [x] テーマ内ファイルは `get_theme_file_uri()` 等で参照している
- [x] サイト内リンクは `home_url()`、`get_permalink()` 等を適切に使用している
- [ ] 開発環境のドメイン、`localhost`、静的HTMLへのリンクが残っていない
- [x] CSSは `wp_enqueue_style()` で読み込まれている
- [x] JavaScriptは `wp_enqueue_script()` で読み込まれている
- [x] PHPテンプレートの `<head>` にページ固有CSSを直書きしていない
- [x] 静的制作用の `include-components.js` を本番テーマで読み込んでいない
- [ ] CSSの管理元が `tokens.css`、`reset.css`、`base.css`、`utility.css`、`component.css`、`page.css` の責務に分かれている
- [ ] ビルド工程がある場合、本番フロント用CSSが1つのバンドルへまとめられている
- [x] ビルド工程がない場合、CSSが依存順にenqueueされ、`@import` で連結されていない
- [ ] ページ別CSSを条件付きで読み込む場合、その必要性と分割基準が記録されている
- [x] enqueueのハンドル名がテーマ固有で、依存関係が正しい
- [x] キャッシュ更新用のバージョン指定方針がある
- [x] ページ固有アセットは必要なページだけで読み込まれる
- [ ] CSS、JS、画像、フォント、ソースマップに404がない
- [ ] 画像パスの大文字・小文字が実ファイルと一致し、Linux環境でも動作する
- [ ] 外部リンク、電話、メール、LINE等のリンク形式が正しい
- [ ] 新しいタブで開くリンクの方針が統一され、安全な `rel` 属性が設定されている
- [ ] パーマリンク設定を変更しても主要ページとアセットが壊れない

## 9. デザイン・CSS

- [ ] 実装前後で `design.md` の方向性を確認している
- [ ] 色、余白、角丸、影、フォントサイズに `asset/css/tokens.css` を優先使用している
- [ ] 追加トークンが既存スケール、命名、役割と整合している
- [ ] primary / secondary / tertiary / accent / surface / semantic の役割が守られている
- [ ] グラデーション、強い色、影、アニメーションを過剰に使用していない
- [ ] `asset/css/reset.css`、`base.css`、`utility.css`、`component.css` の責務が重複していない
- [ ] ページ固有CSSが `asset/css/page.css` または合意した構成で管理されている
- [ ] `.hb__l-container` を利用し、最大コンテンツ幅が `1200px` 以内である
- [ ] `.hb__l-section` と既存バリエーションを再利用している
- [ ] Layout、Component、Project、Utilityの責務が不必要に混ざっていない
- [ ] 共通classが `hb__l-*`、`hb__c-*`、`hb__p-*`、`hb__u-*` に沿っている
- [ ] 独自classがサイト固有プレフィックス `hb-` または `hb__` で始まっている
- [ ] ページ固有classが `hb-{page-slug}__` プレフィックスに沿っている
- [ ] 共有パーツのclassにページ名を含めず、`hb__` プレフィックスを使用している
- [ ] 別ページのslugを持つclassを共有目的で流用していない
- [ ] 複数ページで使う要素を `components/`、`template-parts/`、共有CSSへ適切に昇格している
- [ ] `component.css` 等の共有CSSにページ固有セレクタが混在していない
- [ ] ページ固有CSSに共有パーツ全体の定義を重複していない
- [ ] WordPress標準classを無理に置換していない
- [ ] 同じCSS宣言やマジックナンバーが多数重複していない
- [ ] `!important` の使用が必要最小限で、理由を説明できる
- [ ] CSS詳細度が過剰でなく、エディターやプラグインと不用意に競合しない
- [ ] 未使用CSS、旧静的HTML専用CSS、コメントアウトした大量コードが整理されている
- [ ] 各HTMLのインラインCSSが `asset/css/page.css` へ集約され、ページ名入りclassで競合を防いでいる
- [ ] ソースCSSの分割と本番読込ファイルの構成が文書化されている
- [ ] 印刷が必要なページは印刷表示も確認している

## 10. レスポンシブ・表示品質

- [ ] 320px程度の狭い画面でも横スクロールが発生しない
- [ ] モバイル、タブレット、PC、1200px超の画面幅で確認している
- [ ] 実機または実機相当の環境でiOS SafariとAndroid系ブラウザを確認している
- [ ] Chrome、Edge、Firefox、Safariの対象バージョンで確認している
- [ ] ブラウザの文字サイズ変更や200%ズームで主要操作が可能である
- [ ] 長いタイトル、URL、パンくず、ボタン文言がはみ出さない
- [ ] 表、iframe、動画、埋め込みコンテンツが画面幅を超えない
- [ ] 画像の縦横比が保たれ、意図しない引き伸ばしがない
- [ ] 横並びから縦並びになる際の順序、余白、矢印、CTA位置が自然である
- [ ] 固定ヘッダーや固定CTAが本文、アンカーリンク、管理バーを隠さない
- [ ] WordPress管理バー表示中もヘッダーやモーダル位置が崩れない
- [ ] 画面回転、ブラウザ戻る、ページ再読込後も状態が破綻しない
- [ ] フォント読込前後のレイアウト変動が許容範囲である

## 11. JavaScript・インタラクション

- [ ] ブラウザコンソールにJavaScriptエラーや不要な警告がない
- [ ] JavaScript無効時も主要コンテンツとリンクへアクセスできる
- [ ] 対象要素が存在しないテンプレートでエラーにならない
- [ ] 同じコンポーネントが複数ある場合にすべて正しく動作する
- [ ] 同一IDがページ内に重複していない
- [ ] ハンバーガーメニューの開閉、背景固定、フォーカス移動が正しい
- [ ] タブ、アコーディオン、モーダルの全状態を確認している
- [ ] `aria-expanded`、`aria-selected`、`aria-hidden` 等が表示状態と同期している
- [ ] Escapeキー、Enterキー、Spaceキー、Tabキーで必要な操作ができる
- [ ] アニメーションが操作を妨げず、`prefers-reduced-motion` を考慮している
- [ ] 二重クリックや連打で処理が重複しない
- [ ] `console.log`、デバッグ用アラート、テストコードが残っていない
- [ ] 外部ライブラリに既知の脆弱性や不要な依存がない

## 12. アクセシビリティ

- [ ] ページごとに主要な `<main>` ランドマークが1つある
- [ ] 「本文へ移動」等のスキップリンクがあり、フォーカス時に視認できる
- [ ] `header`、`nav`、`main`、`aside`、`footer` の役割が適切である
- [ ] ページの見出し階層が論理的である
- [ ] 同一ページ内の見出し・フォームラベル・ランドマーク名が分かりやすい
- [ ] 操作可能な要素を `div` や `span` だけで実装していない
- [ ] リンクとボタンが用途に応じて使い分けられている
- [ ] リンク文言が単独で読まれても目的を理解できる
- [ ] 色だけで状態や情報を伝えていない
- [ ] 通常、hover、focus、active、disabledの状態が識別できる
- [ ] キーボードフォーカスを消していない
- [ ] 文字色と背景色のコントラストが十分である
- [ ] 内容画像に適切な代替テキストがある
- [ ] 装飾画像の代替テキストが空になっている
- [ ] フォーム項目に視認可能なラベルが関連付けられている
- [ ] placeholderだけをラベル代わりにしていない
- [ ] 必須、エラー、入力例が支援技術にも伝わる
- [ ] エラーメッセージが対象項目と関連付けられ、修正方法が分かる
- [ ] 自動再生、点滅、時間制限など利用を妨げる表現がない
- [ ] 自動アクセシビリティ検査を行い、結果を目視・キーボード操作で再確認している

## 13. フォーム・外部サービス

- [ ] フォーム機能の実装主体がテーマかプラグインか明確である
- [ ] 送信前、入力エラー、送信中、成功、サーバーエラーの表示を確認している
- [ ] 必須・任意、入力形式、文字数制限が画面と処理で一致している
- [ ] 二重送信対策とスパム対策がある
- [ ] nonce、権限、サニタイズ、エスケープが適切である
- [ ] メール送信先、From、Reply-To、文字化け、迷惑メール判定を確認している
- [ ] 個人情報の保存先、保存期間、通知先が仕様化されている
- [ ] プライバシーポリシーへの同意導線が必要に応じて設けられている
- [ ] reCAPTCHA、地図、動画、解析、SNS等の外部通信とCookie利用が把握されている
- [ ] 外部サービス停止・読込失敗時もページ全体が操作不能にならない
- [ ] APIキーや秘密情報をテーマ内へ直接保存していない

## 14. セキュリティ

- [ ] 動的なテキスト出力に `esc_html()` 等の適切なエスケープがある
- [ ] HTML属性に `esc_attr()` が使用されている
- [ ] URLに `esc_url()` が使用されている
- [ ] 許可するHTML出力に `wp_kses_post()` 等が使用されている
- [ ] エスケープは可能な限り出力直前に行われている
- [ ] `$_GET`、`$_POST`、`$_REQUEST` を未検証で使用していない
- [ ] 入力値が用途に応じてサニタイズ・検証されている
- [ ] 更新処理にnonce検証がある
- [ ] 管理系処理に `current_user_can()` 等の権限確認がある
- [ ] SQLを直接実行する場合に `$wpdb->prepare()` を使用している
- [ ] ファイルパスやリダイレクト先に利用者入力を危険な形で使用していない
- [ ] `unserialize()`、動的include、動的関数実行など危険な処理がない
- [ ] エラー画面にファイルパス、SQL、認証情報などを表示しない
- [ ] リポジトリと配布物に `.env`、鍵、パスワード、個人情報が含まれていない

## 15. SEO・共有・サイト基盤

- [ ] ページごとのタイトルが適切に出力される
- [ ] 主要ページに内容を表す一意な主見出しがある
- [ ] 見出しが見た目だけの目的で乱用されていない
- [ ] canonical、meta description、OGP等をテーマとSEOプラグインで二重出力していない
- [ ] 構造化データを出力する場合、内容と画面表示が一致している
- [ ] 検索結果、404、ページネーションのindex方針が決まっている
- [ ] WordPress標準またはSEOプラグインのXMLサイトマップが意図どおりである
- [ ] パンくずリストの実装主体と構造化データの担当が明確である
- [ ] ステージング環境の検索エンジン非公開設定を確認している
- [ ] 本番公開時に検索エンジンを誤ってブロックしていない
- [ ] favicon、サイトアイコン、OGP画像の設定手順が明確である
- [ ] SNS共有時のタイトル、説明、画像を主要ページで確認している
- [ ] リンク切れと不要なリダイレクトがない

## 16. パフォーマンス

- [ ] 表示に不要なCSSとJavaScriptを全ページで読み込んでいない
- [ ] CSSとJavaScriptの依存関係、読込順、フッター配置が適切である
- [ ] レンダリングを妨げる大きな外部ファイルが整理されている
- [ ] 画像が表示サイズに対して過大でない
- [ ] WordPressのレスポンシブ画像機能を妨げていない
- [ ] 画像に可能な範囲で幅と高さがあり、レイアウトシフトを抑えている
- [ ] ファーストビュー画像の優先読込と、それ以外の遅延読込が適切である
- [ ] WebP等の採用画像が対象ブラウザで表示できる
- [ ] フォントの種類とウェイトを必要最小限にしている
- [ ] 外部フォントの障害時にも読めるフォールバックがある
- [ ] 重いDOM、過剰な入れ子、巨大なインラインHTMLがない
- [ ] 同じデータベースクエリや重い処理を不必要に反復していない
- [ ] キャッシュプラグイン有効・無効の両方で主要機能を確認している
- [ ] Lighthouse等で主要テンプレートを測定し、重大な指摘を解消または記録している
- [ ] 実際のサーバー環境で主要ページの体感速度を確認している

## 17. コード品質

- [ ] PHP構文エラーがない
- [ ] WordPress Coding Standardsを基準に静的解析している
- [ ] HTMLとCSSの重大な構文エラーがない
- [ ] JavaScriptとCSSのlint環境がある場合、エラーがない
- [ ] 関数、定数、フック、enqueueハンドルにテーマ固有の接頭辞がある
- [ ] グローバル変数やグローバル名前空間を不必要に汚染していない
- [ ] 関数名、ファイル名、テンプレートパーツ名が責務を表している
- [ ] PHPのみのファイル末尾に不要な `?>` がない
- [ ] 廃止予定または非推奨のWordPress関数を使用していない
- [ ] TODO、FIXME、仮文言、プレースホルダー、ダミーリンクが整理されている
- [ ] コメントがコードと矛盾していない
- [ ] 変更と無関係なファイル、バックアップ、ビルド生成物が混入していない
- [ ] 改行コード、文字コード、インデントがプロジェクト内で統一されている

## 18. WordPressデバッグ・テストデータ

- [ ] 開発環境で `WP_DEBUG` を有効にして全主要画面を確認している
- [ ] `WP_DEBUG_LOG` にテーマ由来のPHP Warning、Notice、Deprecated、Fatalがない
- [ ] ブラウザコンソールにエラーがない
- [ ] Networkパネルに意図しない404、500、リダイレクトループがない
- [ ] Theme Unit Test Dataまたは同等の境界値データで確認している
- [ ] 長いサイト名とキャッチフレーズでヘッダーが崩れない
- [ ] 長い投稿タイトル、本文、抜粋で崩れない
- [ ] 画像なし、小さい画像、大きい画像、縦長、横長を確認している
- [ ] リスト、引用、表、コード、整形済みテキスト、ギャラリーを確認している
- [ ] コメントを使う場合、深い階層とページ分割を確認している
- [ ] 一覧表示件数を少なくしてページネーションを発生させている
- [ ] パーマリンク形式を変更して主要画面を確認している
- [ ] Theme Checkを実行し、エラーを解消している
- [ ] Theme CheckのWarningとInfoを確認し、対応しない理由を記録している

## 19. 互換性・プラグイン

- [ ] サポート対象の最低WordPressバージョンで確認している
- [ ] サポート対象の最新WordPressバージョンで確認している
- [ ] サポート対象の最低PHPバージョンで確認している
- [ ] 必須プラグインが無効の場合に致命的エラーにならない
- [ ] 必須プラグインが未設定の場合に、管理者が原因を把握できる
- [ ] 必須プラグインの更新後も主要機能が動作する
- [ ] キャッシュ、SEO、フォーム、セキュリティ系の採用プラグインと競合しない
- [ ] プラグイン由来の標準出力を過剰なCSSで壊していない
- [ ] ログイン中、ログアウト中、管理者、編集者など必要な状態を確認している
- [ ] マルチサイト対応が要件ならマルチサイトで確認している
- [ ] 子テーマ対応が要件なら上書き可能性と関数の競合を確認している

## 20. 本番公開・配布物

- [ ] テーマバージョンが今回のリリース内容に合わせて更新されている
- [ ] CSS・JavaScriptのキャッシュバージョンが更新されている
- [ ] 配布物に必要なファイルだけが含まれている
- [ ] `pages/`、`components/`、`temp/` 等の参照・確認用ファイルを本番配布物へ含めるか判断している
- [ ] `.git/`、`.github/`、開発設定、テストデータ、バックアップを配布物へ含めていない
- [ ] ソースマップや未圧縮ソースの配布方針が決まっている
- [ ] `WP_DEBUG` 等の環境設定をテーマから変更していない
- [ ] 本番用の解析タグ、外部サービスID、フォーム通知先が正しい
- [ ] ステージング固有のURL、メールアドレス、noindex設定が残っていない
- [ ] データベースとファイルのバックアップが取得されている
- [ ] ロールバック方法と直前バージョンのテーマファイルが用意されている
- [ ] ステージング環境で最終承認を得ている
- [ ] 本番反映後にキャッシュを適切に削除している
- [ ] 本番でトップ、固定ページ、一覧、詳細、検索、404、フォームをスモークテストしている
- [ ] 本番のエラーログとアクセス状況を反映直後に確認している
- [ ] 公開後の監視期間と不具合連絡先が決まっている

## 21. ドキュメント・引き継ぎ

- [ ] テーマのインストール、有効化、初期設定手順がある
- [ ] 固定ページの作成とテンプレート割当手順がある
- [ ] 表示設定のフロントページ・投稿ページの指定手順がある
- [ ] メニュー作成と表示位置への割当手順がある
- [ ] 必須プラグインの導入・設定手順がある
- [ ] カスタム投稿タイプ、タクソノミー、カスタムフィールドの運用手順がある
- [ ] 画像の推奨サイズ、比率、容量、代替テキストの入力方針がある
- [ ] ブロックエディターで利用するブロック、パターン、注意事項が説明されている
- [ ] 更新してよい箇所と、レイアウト崩れにつながる操作が説明されている
- [ ] フォーム通知先や外部サービスの変更方法が説明されている
- [ ] テーマ更新、バックアップ、復旧方法が説明されている
- [ ] 既知の制限、保留事項、将来対応が記録されている

---

## 最終判定

### リリースを止める重大項目

- [ ] PHP Fatal、JavaScriptの致命的エラー、主要アセットの404がない
- [ ] 主要テンプレートと主要導線に未実装がない
- [ ] 管理画面で入力・更新した内容が正しく公開画面へ反映される
- [ ] エディターCSSが反映され、編集画面と公開画面に重大な乖離がない
- [ ] 入力値のサニタイズ、出力のエスケープ、nonce、権限確認に重大な漏れがない
- [ ] PC・モバイルで主要コンテンツとCTAを利用できる
- [ ] フォームと個人情報の取扱いに重大な不備がない
- [ ] バックアップとロールバック手順がある

### 承認

- [ ] 全項目を確認し、未対応項目には担当者・期限・対応しない理由が記載されている
- [ ] 制作担当者レビュー完了
- [ ] 運用担当者レビュー完了
- [ ] ユーザー最終確認完了
- [ ] 本番公開可

承認者:

承認日:

未対応事項:

---

## WordPress公式参考資料

- [Theme Handbook](https://developer.wordpress.org/themes/)
- [Including Assets](https://developer.wordpress.org/themes/core-concepts/including-assets/)
- [Global Settings and Styles (`theme.json`)](https://developer.wordpress.org/themes/core-concepts/global-settings-and-styles/)
- [Block Stylesheets](https://developer.wordpress.org/themes/features/block-stylesheets/)
- [Accessibility](https://developer.wordpress.org/themes/classic-themes/functionality/accessibility/)
- [Testing](https://developer.wordpress.org/themes/advanced-topics/testing/)
- [Required Theme Files](https://developer.wordpress.org/themes/releasing-your-theme/required-theme-files/)
- [Escaping Data](https://developer.wordpress.org/apis/security/escaping/)
- [Internationalization](https://developer.wordpress.org/themes/advanced-topics/internationalization/)
