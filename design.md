# バイバイコムズ ホビー買取 - デザインガイドライン

> このドキュメントはAIへの指示書です。デザイン実装時に本ガイドラインと `tokens.css` を必ず参照してください。

---

## 1. ブランド概要

| 項目           | 内容                                                           |
| -------------- | -------------------------------------------------------------- |
| サービス名     | バイバイコムズ ホビー買取                                      |
| サービス種別   | ホビー・コレクターズアイテム専門の買取サービス                 |
| ターゲット     | ホビー・フィギュア・カード・ゲームなどを手放したい個人ユーザー |
| 買取方法       | 宅配・らくらく・出張・店頭（すべて手数料無料）                 |
| ブランドの核心 | 「まとめて売れる」「手続きが簡単」「手数料ゼロ」               |

---

## 2. デザインの世界観・トーン

### コンセプト

**「ホビーをまとめて、箱に詰めて、送るだけ」**

大量にあっても、まとめて宅配で手軽に売れることをダイレクトに伝える。手続きの簡単さ・スピード・手数料ゼロを前面に押し出し、「やってみようかな」と思わせるUIを目指す。感情への訴求より、利便性・明快さを優先する。

### トーン＆ボイス

- 簡潔・わかりやすい（手順や条件を迷わず読める）
- スピード感がある（査定・申し込みがすぐできる印象）
- 気軽さがある（大量でも・初めてでも大丈夫という安心感）
- 信頼感がある（手数料無料・全国対応・老舗感）

### 避けるべき方向性

- 過度に派手・チラシ的なデザイン
- 安っぽい蛍光色の多用
- 過剰な「高価買取！！」的な煽りトーン
- 複雑なUIや情報過多なレイアウト

---

## 3. トークンの使用原則

色・スペース・角丸・シャドウはすべて `tokens.css` の変数を使うこと。直値の記述は禁止。

### 色の選び方

- **primary系**: ブランドの「顔」に使う。CTAや見出しアクセントなど、画面内で最も目立たせたい箇所に限定する。多用すると薄まるため、使いどころを絞ること
- **secondary系**: primaryの補助・ホバー状態・アイコンなど、動きや状態変化を表現する場面に使う
- **tertiary（オレンジ）**: 価格・査定額・キャンペーンなど「金銭的な価値・行動喚起」に絞って使う。グリーンと同格に扱わない
- **accent（イエロー）**: 評価・ハイライトなど補助的なアクセントのみ。単独でCTAに使わない
- **surface系**: 複数の変数があるが、階層の深さで使い分ける。ページ背景→セクション背景→カード背景の順に明度を上げていくイメージ
- **semantic（success/warning/error/info）**: システムメッセージ・フォームバリデーションのみに使用し、デコレーション目的には使わない

### タイポグラフィの判断基準

- フォントサイズは `tokens.css` の `--hb-font-size-*` 変数のみ使用し、独自値を追加しない
- 見出しにグリーン系の色をつける場合は `--hb-color-text-deep` を基本とし、primaryカラーはアクセントの一部（アンダーライン・ハイライト等）に留める
- フォントは `"Noto Sans JP", sans-serif` 固定

### スペーシングの判断基準

- コンポーネント内部の余白: `xs`〜`lg`
- コンポーネント間・セクション内の余白: `xl`〜`2xl`
- セクション間の余白: `3xl`
- この3段階の区別を守ることで、視覚的なリズムが生まれる

### 角丸の判断基準

- pill形状（`radius-full`）はボタンとバッジのみ。カードやモーダルに使うと過剰に丸くなる
- カード類は `radius-sm`〜`radius-md` を基本とし、入れ子になる要素はひと回り小さい値を使う（例：カードが `radius-md` なら内側の画像は `radius-sm`）

### グラデーションの判断基準

- グラデーションは「ここぞ」の場面（ヒーロー・メインCTA）に限定する
- 複数の要素に同時に使うと安っぽくなるため、1画面内で使う箇所は原則1〜2箇所まで

---

## 4. コンポーネント設計方針（構造・状態の定義）

### セクション

- セクション全体：`.hb__l-section`（またはそのバリエーション）を使用する
- 汎用コンテナ：`.hb__l-container`（またはそのバリエーション）を使用する

### ボタン

- プライマリ: `--hb-gradient-primary` 背景 + 白テキスト + `--hb-radius-full`
- セカンダリ: 白背景 + `--hb-color-primary-500` ボーダー＆テキスト
- テキスト系: アンダーライン or グリーンテキストのみ

### カード

- 背景: `--hb-color-white`
- ボーダー: `--hb-color-border-muted` または なし
- 角丸: `--hb-radius-md`
- シャドウ: `--hb-shadow-sm`

### バッジ・ラベル

- 手数料無料・全国対応などの強調: `--hb-color-surface-pill` 背景 + `--hb-color-primary-600` テキスト
- 価格・査定額: `--hb-color-tertiary-500` アクセント

### フォーム・入力

- ボーダー: `--hb-color-border-muted`
- フォーカス: `--hb-color-primary-500` ボーダー
- 角丸: `--hb-radius-md`

---

## 5. 実装ルール

1. **CSS変数の直値禁止**: すべての色・スペース・角丸は `tokens.css` のトークンを使用する
2. **フォント固定**: `"Noto Sans JP", sans-serif` 以外を使用しない
3. **レスポンシブ**: モバイルファーストで設計する。コンテナは `--hb-container` を上限に、左右に `--hb-space-md` のパディングを確保する
4. **アクセシビリティ**: コントラスト比 4.5:1 以上を維持する。テキストに `--hb-color-text-muted` を使う場合は背景色との組み合わせに注意
5. **LINEカラー**: LINE連携UIには `--hb-color-line`（`#06c755`）を使用する

---
